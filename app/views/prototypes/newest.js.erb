$('.proto-list .row').html(
  '<%= j(render partial: 'prototypes/prototype', collection: @prototypes) %>'
);
$('li.sort_tab.newest').addClass('active');
$('li.sort_tab.popular').removeClass('active');

